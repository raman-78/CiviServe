# Development-only dependencies. Install after the runtime deps:
#   pip install -r requirements.txt -r requirements-dev.txt
# (Windows PowerShell)
#   pip install -r requirements.txt; pip install -r requirements-dev.txt

# Testing
pytest>=8.3.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0.0
httpx>=0.27.0
aiosqlite>=0.20.0

# Linting / typing
ruff>=0.9.0
mypy>=1.14.0
types-psycopg>=3.2.0

# Dev server reload companion (uvicorn --reload)
watchfiles>=1.0.0